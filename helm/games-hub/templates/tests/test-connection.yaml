apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "games-hub.fullname" . }}-test-connection"
  labels:
    {{- include "games-hub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "games-hub.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
