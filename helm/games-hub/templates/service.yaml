apiVersion: v1
kind: Service
metadata:
  name: {{ include "games-hub.fullname" . }}
  labels:
    {{- include "games-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "games-hub.selectorLabels" . | nindent 4 }}
